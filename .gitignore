# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-pwa-base-check
*.local

# Local data & secrets — never publish
data/
*.db
*.sqlite
.env
.env.*
!.env.example

# Local scratch space — never publish
scratchpad/

# QA screenshots are working artifacts, never committed (REPORT.md and other
# text reports in the same folder ARE committed)
docs/screenshots/**/*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code permissions for the auto-iteration loop
.claude/settings.local.json
