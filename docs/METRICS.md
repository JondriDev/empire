# The Empire — Fitness Metrics (the potential field)

> **Why this file exists (first principles):** you cannot descend a gradient you
> don't measure. Before this, "better" was a vibe ("feels like alien technology"),
> so the fleet polished whatever was nearest and every run was a slight iteration.
> These north-star numbers turn "huge iteration" into something *measurable*: each
> run reports its delta, the **Strategist** ranks epics by steepest gradient, the
> **Reviewer** uses them as a no-regression gate, and the **Digest** plots the trend.

The machine-measurable rows are computed by [`scripts/metrics.mjs`](../scripts/metrics.mjs)
(dependency-free; run `node scripts/metrics.mjs`). It updates `docs/metrics.json`
(current snapshot + rolling history) and prints a table with deltas. **Build first**
(`npm run build`) if you want the bundle-size row populated.

## Auto metrics (from `scripts/metrics.mjs`)

> **✅ 2026-07-11 (green main `5e37d8d`, EPIC-14 S1 independent QA, `metrics.json` this run) — ratchet HOLDS, all-0; new `offShellControls` baseline reproduced.** The three design-system axes stay locked through S1's new `ui` primitives: `tokenViolations` **0**, `offSystemUtilities` **0**, `offSystemStyle` **0 (r0/t0/m0)** — `node scripts/metrics.mjs --assert-zero` **exits 0**. The new control-shell axis reproduces the S1 baseline exactly: `offShellControls` **341 (b271/i48/s6/t16)**. Live values this run: **apps 31**, **test cases 399**, **test files 49**, **bundle gz 729.8** (Δ ±0 vs the committed S1 snapshot — HEAD is the S1 commit, so no app code landed since; this QA moved nothing). Vs the prior QA (EPIC-13, 2026-07-10): +8 test cases / +1 file (S1's `ui.test.tsx`), `offShellControls` row born. The row values below reflect this snapshot.

> **✅ 2026-07-10 (green main `91ceaec`, EPIC-13 CODE-COMPLETE re-confirm, `metrics.json` prior run) — ratchet HOLDS, all-0.** The mail+crypto
> design-system regressions the 2026-07-09 run flagged are FIXED and stay fixed: `tokenViolations` **0**,
> `offSystemUtilities` **0**, `offSystemStyle` **0 (r0/t0/m0)** — `node scripts/metrics.mjs --assert-zero` **exits 0**.
> Live values that run: **apps 31**, **test cases 391**, **test files 48**, **bundle gz 729.8**.

| Metric | Current (QA 2026-07-10, green main `a9bec85` — **EPIC-13 CODE-COMPLETE render-CONFIRMED `GRAPH-LEGIBLE 3/3` + `INBOUND-LANDS 4/4`; `--assert-zero` exit 0; all Δ ±0, no drift**) | Target | Direction |
|---|---|---|---|
| Apps / routes | **31** (metrics.mjs; smoke↔registry exact at 31; 32/32 incl. desktop shell render clean) | ~26 (steady) | coherence over new surface — not a growth metric |
| Test cases | **391** (metrics.mjs, `src/` static; +15 since the S2 snapshot — EPIC-13 S3 draftStore/mailGraph + music a11y tests) · vitest run higher | 60+ | ↑ higher = safer to leap |
| Test files | **48** (metrics, `src/` static — +3 vs the S2 snapshot: `draftStore.test.ts`, `mailGraph.test.ts`, `Music.test.tsx`) | grow with code | ↑ |
| Design-token violations | **0** | 0 | ↓ raw hex/rgb in app code that bypasses the design system |
| Off-system utilities | **0** (↓ from 1076 — the redesign batch's `98c61c7` "token-ize Tailwind palette classes across all apps" swept the whole mass; EPIC-5 S8 `c51f79f` LOCKED it with `--assert-zero` CI gate; re-confirmed 0 this run) | 0 | ↓ Tailwind palette classes (`text-gray-400`, `bg-cyan-600`, `bg-white/10`, `text-white`, `text-red-400`…) that bypass the JondriDev tokens — **EPIC-5 TARGET MET (0)** |
| Off-system style | **0** (r0/t0/m0 — **the full −56 leap from the S1 baseline of 56; EPIC-11 TARGET MET (0)**; **QA-CONFIRMED independently 2026-07-05 on green main `4c643a9`** — `metrics.mjs` reproduces `0 (r0/t0/m0)` exactly and **`--assert-zero` exits 0**. S2 drove type t42→0, S3 drove radii r12→0, **S4 drove motion m2→0 and LOCKED it in `--assert-zero`** (mirrors the EPIC-5 S8 offSystemUtilities lock). All three sub-counts 0 and gated — cannot regress. **S1–S4 done-confirmed; EPIC-11 CODE-COMPLETE, ready to retire to DONE.** | 0 | ↓ raw radii/type/easing bypassing `--radius-*`/`--text-*`/`--ease-*` |
| Off-shell controls | **341** (b271/i48/s6/t16 — **NEW baseline, EPIC-14 S1 2026-07-11**; bare `<button>`/`<input>`/`<select>`/`<textarea>` in app code bypassing the `ui` primitive layer, over the same `appCodeFiles()` set minus `src/components/ui/`; `type=file/checkbox/radio` inputs exempt). Detector = pure `scanControlViolations` (`scripts/controlAudit.mjs`, 13 cases). **NOT yet in `--assert-zero`** — EPIC-14 S2–S8 drive it to 0 heaviest-first, S9 locks it. | 0 (then LOCK) | ↓ bare controls bypassing `ui` `Button`/`IconButton`/`Input`/`TextArea`/`Select`/`Segmented` |
| Bundle gz (KB) | **729.8** (Δ ±0 vs the committed snapshot — EPIC-13 S3 + playwright devDep already landed here; playwright is dev-only, never enters the prod bundle; no new shipped deps) | hold / shrink | ↓ |

> **Off-shell controls (added 2026-07-11 — design-system conformance III).** The colour audits
> (`tokenViolations`/`offSystemUtilities`) and the style audit (`offSystemStyle`) all sit at **0**, but nothing
> measured the **component-shell axis**: whether an app renders its interactive controls through the `ui` primitive
> layer or drops to a bare `<button>`/`<input>`/`<select>`/`<textarea>` (which bypasses the shell's glass surface,
> focus ring, spring motion, and a11y affordances — the exact drift that made Mail+Crypto raw-HTML islands).
> `offShellControls` measures that third axis. Detector = the pure, unit-pinned `scanControlViolations`
> (`scripts/controlAudit.mjs`, 13 cases in `controlAudit.test.mjs`) over the same app-code file set the colour/style
> audits walk, minus `src/components/ui/` (its primitives legitimately render the bare elements they wrap). The row
> reports `b/i/s/t` = button/input/select/textarea sub-counts. **`type=file|checkbox|radio` inputs are exempt** (no
> text-field primitive home). EPIC-14 completed the `ui` set (`Select`/`IconButton`/`Segmented`) so every control now
> has a shell home; S2–S8 migrate the 54 offender files by descending mass, S9 adds `offShellControls` to
> `--assert-zero` to lock it at 0 — exactly as EPIC-5 S8 locked `offSystemUtilities` and EPIC-11 S4 locked `offSystemStyle`.

> **Off-system style (added 2026-07-04 — design-system conformance II).** The two colour audits
> (`tokenViolations` = raw `#hex`/`rgba()`; `offSystemUtilities` = Tailwind palette classes) are both at **0**,
> but app code still hardcodes the NON-colour design values — **radii, type sizes, easings** — instead of
> consuming `var(--radius-*)`/`var(--text-*)`/`var(--ease-*)`. `offSystemStyle` measures that second axis. The
> detector is the pure, unit-pinned `scanStyleViolations` (`scripts/styleAudit.mjs`, 16 cases in
> `styleAudit.test.mjs`) over the same app-code file set the colour audits walk (DS-infra allowlisted). The row
> reports `r/t/m` = radii/type/motion sub-counts. **Scope decision (so it's DRIVEABLE):** raw **spacing**
> (padding/margin/gap px) is deliberately EXCLUDED — unlike the scale-backed radii/type/easing it has too many
> legitimate one-off geometry values (an 8px status dot) with no bounded token-only target. Reduction is
> decomposed into follow-on stages by descending file mass (see `docs/EPICS.md`); once at 0 a future stage can
> add `offSystemStyle` to `--assert-zero` to lock it, exactly as EPIC-5 S8 locked `offSystemUtilities`.

> **Off-system utilities (added 2026-06-29).** `tokenViolations` only ever counted raw `#hex`/`rgba()`
> *literals*, so it hit 0 while ~1,160 ergonomic-but-off-system Tailwind palette classes still bypassed
> the design system (and broke theme-switching, since `text-white`/`bg-gray-*` don't follow `[data-theme]`).
> Migrate them to the token-backed utilities added in `src/design-system/theme.css`
> (`text-fg`/`text-muted`/`text-faint`, `bg-glass`, `border-hair`, `text-signal`/`bg-signal`,
> `text-success`/`text-warn`/`text-danger`/`text-info`), the `.gp`/`.glass` primitives + `src/components/ui`
> components, or `cssVar()`/`tint()`/`CATEGORICAL` for JS-computed colours. The lock stage flips
> `node scripts/metrics.mjs --assert-zero` into a hard gate so it can't regress.

> Last integration since prior QA snapshot (`d17f73a`, EPIC-4 S4, apps 25 / gz 292.5 / vitest 205 / off-system
> 1076): the **out-of-band redesign batch** (`75ef685`…`fb4c853`, 2026-06-30, user-directed) + **EPIC-5 S8
> close** (`c51f79f`) landed. The batch: **full-screen "Apple-style" app model** (`src/components/Window.tsx`
> deleted, new `AppHost.tsx`/`Recents.tsx`/`cakraTab.ts`); **Prompt-Gen / Token-Counter / Code-Editor folded
> into Cakra** as tabs; a new **Reader** app (EPUB/PDF/TXT/MD/DOCX, Cakra-powered); and `98c61c7`
> "token-ize Tailwind palette classes across all apps" which drove **off-system 1076 → 0**. Δ vs `d17f73a`:
> apps **25 → 26** (+1, Reader net), test cases **205 → 208 vitest** / 163 → 166 static (+3), test files **23 →
> 24 vitest** / 21 → 22 metrics (+1), token violations ±0 (**0**), **off-system 1076 → 0 (−1076, EPIC-5 target
> met + LOCKED)**, bundle gz **292.5 → 691.3** (+398.8, the Reader's parser libs — BY DESIGN). CONFIRMED on green
> main `c51f79f`, no contradiction, no runtime bug. **★ This run is the FIRST visual QA of the redesign batch —
> all 26 routes render clean incl. the new windowless shell + Reader + the merged Cakra tabs.** EPIC-5
> (design-system utility conformance → off-system 0) **CONFIRMED MET & LOCKED** (`node scripts/metrics.mjs
> --assert-zero` exits 0: tokenViolations=0, offSystemUtilities=0). EPIC-4 (PWA) re-confirmed: offline-boots
> 5/5, PRECACHE no-gap (70 entries / 43 JS + 3 CSS). **Next: NO active epic — EPIC-5 CLOSED; Strategist must
> promote the next epic** (EPIC-6 Android is device-gated/QUEUED; cloud-executable candidates: DataCenter/Files
> whole-state graph-mirror, organism-completeness-II re-audit vs the new 26-route registry).

> Prior context (still load-bearing): the **JondriDev redesign** (`bf76cf5`…`23df6ce`) intentionally set apps
> **27 → 25** (deleted `ai-agent` + `hermes-cc`; AI unified into **Cakra** at `/app/ai-chat`) and bundle gz
> **248 → 288.6** (real **Leaflet + OSM** Maps); palette swapped XENO → Earth-from-Space, alien SVG icon set,
> decorative HUD removed. **Do NOT regress-gate these intentional deltas.** It also made
> **Weather/Maps/Language/DataCenter genuinely work** (`b155992`) — pre-delivered the first four EPIC-3 instruments.

## Manual / CI metrics (QA + human)

| Metric | Source | Current (QA 2026-07-04, on green main `ad9c734` — **EPIC-11 S1 · design-system conformance II audit + baseline**, first independent confirm) | Target |
|---|---|---|---|
| Routes rendering clean | QA `REPORT.md` (headless render, no uncaught JS / blank) | **✅ 2026-07-11 (green main `2622813`) — EPIC-14 S2+S3 render-CONFIRMED (first QA since both migrations shipped). 32/32 routes render clean (desktop + all 31 registry apps), 0 uncaught / 0 console errors across every screenshot. All 13 guard suites green: INBOUND 4/4, MEDIA 3/3, GRAPH-LEGIBLE 3/3, GLOBAL-SEARCH 1/1, NODE-LINEAGE 1/1, INTENT-ROUNDTRIP 2/2, TIMELINE 1/1 (6 axes), HOME-ALIVE 1/1, PROVENANCE 3/3+3/3, PRECACHE 91 no-gap, OFFLINE 5/5. `--assert-zero` exit 0 (colour/util/style axes all 0). **Active-epic target metric `offShellControls` reproduced EXACTLY: 307 (b243/i48→i44/s6/t14), Δ ±0 vs committed snapshot — both S2 (Reader 341→322) and S3 (Calendar 322→307) acceptances CONFIRMED, net −34.** Reader + Calendar both dropped off the top-offenders list (now FormBuilder 16, Calculator 14, DataCenter 14, AIChat 13, Maps 12, Photos 12, Clock 11, Goals 10). Auto-metrics Δ ±0 (apps 31, test cases 411, files 52, bundle gz 730.2). Visually inspected: `desktop.png` (Bridge "Good morning" + 4 stat cards + full 32-tile launcher Cakra→Crypto), `app-reader.png` (S2 shelled — amber "Add book" + "+ Add your first book" primary Buttons, clean empty-state), `app-calendar.png` (S3 shelled — "Today" pill Button, month chevron IconButtons, solid teal "+ Add Event" primary Button), `app-mail.png` (graceful "Provider himalaya not configured", no error boundary), `app-maps.png` (real Leaflet container, tiles env-blocked). ▶ NEXT = **S4 (Clock 13 + Photos 12 → 0)**. No runtime bug, no drift. — Prior: ✅ 2026-07-11 (green main `5e37d8d`) — EPIC-14 S1 independent QA. 32/32 routes render clean (desktop + all 31 registry apps), 0 uncaught / 0 console errors / 0 page errors across every screenshot. All 14 guard suites green: INBOUND 4/4, MEDIA 3/3, GRAPH-LEGIBLE 3/3, GLOBAL-SEARCH 1/1, NODE-LINEAGE 1/1, INTENT-ROUNDTRIP 2/2, TIMELINE 1/1 (6 axes), HOME-ALIVE 1/1, PROVENANCE 3/3+3/3, PRECACHE 91 no-gap, OFFLINE 5/5. `--assert-zero` exit 0. New `offShellControls` baseline reproduced exactly: **341 (b271/i48/s6/t16)**; colour/style axes all 0. Auto-metrics Δ ±0 vs the committed S1 snapshot (apps 31, test cases 399, files 49, bundle gz 729.8). Visually inspected: `desktop.png` (Bridge "Good night" + 4 stat cards + full 31-tile launcher), `app-mail.png` (shelled — envelope glyph, Himalaya/AgentMail toggle, graceful "Provider himalaya not configured", NO error boundary), `app-crypto.png` (Wallet glyph + 5 shelled BTC/ETH/SOL/XRP/DOGE inputs), `app-maps.png` (real Leaflet container, tiles grey — env-blocked). **EPIC-14 S1 acceptance CONFIRMED (baseline established + measured); `offShellControls` holds at 341 by design — S1 drives nothing, S2 (Reader 19→0) is the first mover.** No runtime bug, no drift. — Prior: ✅ 2026-07-10 (green main `a9bec85`) — ★ EPIC-13 CODE-COMPLETE re-CONFIRMED on the post-playwright-devDep tree. 32/32 routes render clean (desktop + all 31 registry apps, 0 uncaught, 0 console errors). All 13 guard suites green: INBOUND 4/4, MEDIA 3/3, GRAPH-LEGIBLE 3/3 (reader/book + crypto/wallet + mail/draft), GLOBAL-SEARCH 1/1, NODE-LINEAGE 1/1, INTENT-ROUNDTRIP 2/2, TIMELINE 1/1 (6 axes), HOME-ALIVE 1/1, PROVENANCE 3/3+3/3, OFFLINE 5/5, PRECACHE 91 no-gap. `--assert-zero` exit 0. Metrics all Δ ±0 vs committed snapshot (apps 31, test cases 391, files 48, bundle gz 729.8). Visually inspected: `desktop.png` (Bridge "Good night" + 4 stat cards + full grid ending Search·Timeline·Mail·Crypto, bespoke glyphs no `Node` fallback); `app-music.png` (shelled Music Player, compact "No track playing" empty state — the a11y polish landed no visual regression); `app-mail.png` (envelope glyph, Himalaya/AgentMail toggle, graceful "Provider himalaya not configured." NO error boundary); `app-crypto.png` (gold Wallet glyph + 5 labelled BTC/ETH/SOL/XRP/DOGE inputs). No runtime bug, no drift. ▶ Still awaiting Strategist to retire EPIC-13 → DONE + promote next epic. — Prior: ✅ 2026-07-10 (green main `5419079`) — ★ EPIC-13 CODE-COMPLETE render-CONFIRMED. 32 / 32 routes render clean (desktop + all 31 registry apps), 0 uncaught. `GRAPH-LEGIBLE 2/2 → 3/3 ✅` reproduced independently — all three axes pass (reader/book + crypto/wallet + the new **mail/draft**: seed `empire-mail-drafts` before mount → a `draft` node owned by `app==='mail'` survives a 2nd reload). **EPIC-13 S3 acceptance metric MOVED (2/2→3/3) → S1–S3 all done-confirmed → EPIC-13 CODE-COMPLETE.** `INBOUND-LANDS 4/4 ✅` (mail|notes holds). Visually confirmed: `desktop.png` (Bridge home + full 31-tile grid ending in Search·Timeline·Mail·Crypto, both bespoke glyphs); `app-mail.png` (Mail shelled — envelope glyph, Himalaya/AgentMail toggle, graceful "Provider himalaya not configured." on the 401, NO error boundary); `app-crypto.png` (Wallet glyph + 5 mono BTC/ETH/SOL/XRP/DOGE inputs); `app-network.png` (CORE mesh, legend now includes **`draft`** — the S3 node type joined the organism). All 13 guard suites green (INBOUND 4/4, MEDIA 3/3, GRAPH-LEGIBLE 3/3, GLOBAL-SEARCH 1/1, NODE-LINEAGE 1/1, INTENT-ROUNDTRIP 2/2, TIMELINE 1/1 all 6 axes, HOME-ALIVE 1/1, PROVENANCE 3/3+3/3), OFFLINE 5/5, PRECACHE 91 no-gap, `--assert-zero` exit 0. Metrics all Δ ±0 vs committed snapshot (apps 31, test cases 386, files 47, bundle gz 729.5). No runtime bug, no drift. ▶ Ready for Strategist to retire EPIC-13 → DONE + promote next epic. — Prior: ✅ 2026-07-10 (green main `1a8c2f7`) — EPIC-13 S1 render-confirm: 32/32 clean, `GRAPH-LEGIBLE 2/2 ✅` (crypto/wallet axis), Crypto shelled, all 12 guards green, `--assert-zero` exit 0. No drift. — Prior: ✅ 2026-07-09 (green main — post-fix) — 32 / 32 routes render clean (desktop + all 31 registry apps), 0 uncaught. Both prior regressions CONFIRMED FIXED live: `mail` now renders "Provider himalaya not configured." with NO error boundary (`app-mail.png` re-confirmed — closes the owed re-confirm); the design-system ratchet is back to 0 and `metrics.mjs --assert-zero` exits 0 (was exit 1 / 2 token + 4 style on RED `17d2dd9`). All 12 guard suites green, `INTENT-ROUNDTRIP 2/2 ✅` (EPIC-12 acceptance holds on green main), OFFLINE 5/5, PRECACHE 91 no-gap. Visually re-verified desktop (Bridge, 31-tile grid), mail (graceful not-configured), crypto (Wallet Watch form), network (CORE mesh + legend). No runtime bug, no drift. — Prior: 🔴 2026-07-09 (green build, main `17d2dd9`) — 30 / 31 registry routes render clean; `mail` FAILS (error boundary — runtime bug: `Object.entries(status.providers)` at `Mail.tsx:63` throws on a 401 status response with no `providers` key; see REPORT.md top banner). Also `metrics.mjs --assert-zero` exit 1: the new mail+crypto apps reintroduced 2 token + 4 off-system-style (r0/t4/m0) violations — the design-system ratchet that was LOCKED at 0 is BROKEN. REGISTRY-COVERAGE now 31 apps (I added `mail`,`crypto` to the smoke list — they were shipped un-smoked). `INTENT-ROUNDTRIP 2/2 ✅` (EPIC-12 acceptance holds). Desktop + crypto + the other 29 apps render clean. Both regressions are new-app src/ fixes → flagged for the build routine, not patched here. — Prior confirm: 29 / 29** ✅ (30/30 incl. desktop shell; SHELL-IS-STYLED ✅ + REGISTRY-COVERAGE ✅ bidirectional, 29 apps; INBOUND-LANDS 3/3 ✅ + **MEDIA-PERSISTS 3/3 ✅ (music + video + photos)** + **GRAPH-LEGIBLE 1/1 ✅** + **GLOBAL-SEARCH 1/1 ✅ (`tagOnly=true`)** + **NODE-LINEAGE 1/1 ✅ (5 axes)** + **TIMELINE 1/1 ✅ (all 6 axes — `ordered`+`grouped`+`flow`+`persisted`+`filtered`+`descendants`)** + **HOME-ALIVE 1/1 ✅ (The Bridge)** + **PROVENANCE-PERSISTS 3/3 ✅** + **PROVENANCE-ENTITY 3/3 ✅** + OFFLINE-BOOT 5/5 ✅ / PRECACHE 85 entries NO GAP) — **confirmed 2026-07-05 on green main `57262e8`** (EPIC-11 S2 `20bc957` + S3 `4f79ded` + Cakra Problem Solver `57262e8` landed since last QA `ca10d0a`): all 30 routes render with 0 uncaught JS incl. the **new `solver` route**, vitest **360/360** (38 files, +3 solver test files), all guards green, `metrics.mjs --assert-zero` exit 0. Visually verified: the **Bridge** home (`desktop.png` — "Good night", 4 live stat cards, the 25-tile launcher grid through **Timeline**) + the **Problem Solver** (`app-solver.png` — "Problem Solver · AI", `0 open · 0 solved · today 0/100 AI calls`, **Import world catalog (32)** button, `daily budget 100` + **Solve everything**, the green-puzzle "The world has problems" empty state) + all 29 app routes incl. **Maps** real Leaflet container (only tiles grey — env-blocked, net:8). No runtime regression; product metrics reflect S2+S3 (offSystemStyle 56→**2** r0/t0/m2) + the solver (apps 28→29, vitest 334→360, bundle 705.4→718.9). **↻ RE-CONFIRMED 2026-07-05 on green main `6d983b3`** (the empty-state refactor — 5 apps unified onto the shared `<EmptyState>`, landed after the last QA `0b7af75`/`4c643a9`; this is its first headless render-QA): all **30/30 routes render clean** incl. the 5 refactored apps (`datacenter`/`inbox`/`messages`/`photos`/`reader`, 0 uncaught), every guard green (**PRECACHE 86 no-gap** — +1 chunk from the refactor), test cases 307/36 files, offSystemStyle **0 (r0/t0/m0)** ±0, `--assert-zero` exit 0, bundle 717.4 ±0. Visually confirmed the unified empty state (`app-messages.png` "No messages with Jondri yet"). No runtime bug, no regression. **↻ RE-CONFIRMED 2026-07-06 on green main `071a749`** (the empty-state unification *completed* — a compact `size="sm"` `<EmptyState>` variant + 8 remaining spots, adoption 6→13; the Builder landed it mid-QA-run, so I reset to `071a749` and re-built + re-smoked the tree actually being pushed): all **30/30 routes render clean** (0 uncaught), every guard green (REGISTRY-COVERAGE **29** smoke↔registry exact, PRECACHE 86 no-gap), test cases **309**/36 files, offSystemStyle **0 (r0/t0/m0)** ±0, `--assert-zero` exit 0, bundle **717.6** ±0 — every metric Δ ±0 vs the committed snapshot. Visually confirmed the compact `size="sm"` empty state (`app-music.png` "No track playing · Add audio files to get started") + clean Language Lab / Maps / Browser / Bridge / Solver. The polish reintroduced no raw radii/type/easing → EPIC-11 stays QA-confirmed. No runtime bug, no drift. **↻ RE-CONFIRMED 2026-07-06 on green main `7e68e1c`** (the Strategist reshape: EPIC-11 retired to DONE, deps-audit gate `check-audit.mjs` + xmldom-override patch landed, **EPIC-12 · Intent integrity promoted, S1 pending** — landed since last QA `67afd26`): all **30/30 routes render clean** (0 uncaught), every guard green (REGISTRY-COVERAGE **29** smoke↔registry exact, INBOUND 3/3, MEDIA 3/3, GRAPH-LEGIBLE 1/1, GLOBAL-SEARCH 1/1, NODE-LINEAGE 1/1, TIMELINE 1/1 all 6 axes, HOME-ALIVE 1/1, PROVENANCE 3/3+3/3, OFFLINE 5/5, **PRECACHE 86 no-gap**), test cases **309**/36 files, **offSystemStyle 0 (r0/t0/m0)** ±0, `--assert-zero` exit 0, bundle **718.3** ±0 — every metric Δ ±0 vs the committed snapshot. Visually re-verified the Bridge (`desktop.png`), Network CORE mesh (`app-network.png`), Solver green-puzzle empty state + live world feed (`app-solver.png`). **EPIC-12 S1 not yet shipped → no `INTENT-ROUNDTRIP` guard yet, nothing to confirm; the `sync.ts` phantom-entity bug is still present on this tree.** No runtime bug, no drift. **↻ RE-CONFIRMED 2026-07-06 on green main `94ff5f1`** (EPIC-12 **S1 + S2 both landed** since last QA `7e68e1c` — `make-note-from` `92380dc` + `add-to-learning` `94ff5f1`; first headless drive of either intent): all **30/30 routes render clean** (0 uncaught), every guard green (REGISTRY-COVERAGE **29** smoke↔registry exact, INBOUND 3/3, MEDIA 3/3, GRAPH-LEGIBLE 1/1, GLOBAL-SEARCH 1/1, NODE-LINEAGE 1/1, **INTENT-ROUNDTRIP 2/2 ✅**, TIMELINE 1/1 all 6 axes, HOME-ALIVE 1/1, PROVENANCE 3/3+3/3, OFFLINE 5/5, **PRECACHE 86 no-gap**), test cases **323**/37 files, **offSystemStyle 0 (r0/t0/m0)** ±0, `--assert-zero` exit 0, bundle **718.6** ±0. **The `add-to-learning` axis first read `stored=false` — traced to a GUARD bug, NOT a product regression: the guard matched the learning item's `from` against the store note id, but the intent honestly writes `from`=the note MIRROR's graph-node id (reconcile keeps the store id only in `data.sourceId`, and `NodeActions` hands the intent the graph node). Fixed the guard to resolve + match the mirror id (`scripts/qa-smoke.mjs`, my harness); the feature itself writes a real `learningItems` entry + `learning` mirror, both reload-durable — verified by direct probe.** Visually confirmed desktop (styled Bridge + full grid) + learning-tracker (clean `<EmptyState>`). **EPIC-12 S2 done-confirmed; S3 (LOCK) is the last stage.** No runtime bug, no drift. | 26 / 26 (every entity route) |
| Shallow instruments with offline function + a unit test (EPIC-3 target) | QA + code audit | **8 / 8 ✅ PRIMARY METRIC HIT** — Clock (S1: `empire-clock-state` persistence + `clockLogic.test.ts` 17 cases ✅) + Music + Video (S2: real `Blob`s in IndexedDB via `mediaStore.ts`, metadata-only localStorage, ghost-drop on rehydrate + `mediaStore.test.ts` 11 cases ✅) + **Photos (S3: same `mediaStore` IDB rail, `photosStore.test.ts` 6 cases ✅ — live IDB roundtrip CONFIRMED this run by the extended MEDIA-PERSISTS `photos` case: add image → reload → survives)** + the 4 redesign instruments Weather/Maps/Language/DataCenter (function ✅; DataCenter+Weather dedicated tests pending → S4 backfills, Maps/Language render-smoke-covered). **Moved 7/8 → 8/8 this run (S3 confirmed live). All eight shallow instruments now offline-capable.** | 8 / 8 ✅ |
| Apps fully wired into the organism (both **emit** and **receive** honest handoffs, visible in The Network) | QA + code audit | **10 / 10 entity-apps-with-inbound ✅ TARGET HIT** + **every collection-owning app now graph-legible (Mail closed — EPIC-13 S3; the LAST island)**. Both-ways: `prompt-generator`, `notes`, `learning-tracker`, `editor`, `token-counter`, `ai-chat`, `calendar`, `goals`, `messages`, **`mail`** (EPIC-13 S2 — receives via `useInboundHandoff('empire-mail-clipboard')` → prefilled compose + "From <source>" `ProvenanceChip`; emits Send-to-Mail). Each: natural text→entity inbound via `useInboundHandoff` → prefilled create form + `ProvenanceChip`. **Emit-only sources ALL legible in the graph** — Reader (`book`, S4), **Crypto (`wallet`, EPIC-13 S1)** and **Mail (`draft`, EPIC-13 S3 — the last graph-island, now closed)**. `GRAPH-LEGIBLE` guard **3/3 ✅ this run** (reader/book + crypto/wallet + mail/draft — each a typed node owned by its app in `empire-core-graph`, surviving reload). Both islands also EMIT onward via per-item ⚡ `<NodeActions>`. Intentionally emit-only by design (no natural inbound): files, photos, datacenter, reader, crypto (browse/manage stores) + tool apps via `NodeActions`. | 10 / 10 entity-apps-with-inbound ✅ + all collections graph-legible ✅ |
| **EPIC-6 target — durable app→app provenance persists across reload (`PROVENANCE-PERSISTS`) + per-entity source (`PROVENANCE-ENTITY`) + Reader graph-legible (`GRAPH-LEGIBLE`)** | QA `qa-smoke.mjs` guards + visual + code | **✅ EPIC-6 DONE — S1 SPINE + S2 SURFACE + S3 PER-ENTITY + S4 READER-ISLAND ALL CONFIRMED LIVE 2026-07-02 (green main `e262f1b`).** `PROVENANCE-PERSISTS` 3/3 (edge ledger): 3 REAL `editor→<target>` handoffs from the Editor's ⚡ Send menu → recorded in `empire-provenance` by `startProvenanceTracking()`/`flowForEvent` → survive a full reload. `PROVENANCE-ENTITY` 3/3 ({calculator→goals, editor→messages, notes→calendar}: seed inbound → reload+prefill → app's OWN create/send → SECOND reload → `<LineageTrail>` still renders off the persisted entity). **S4 (EPIC-6 CLOSE) NOW done-confirmed via the NEW `GRAPH-LEGIBLE` guard — 1/1:** Reader's real file `<input>` driven with a `.txt` book → a `book` CoreNode owned by `app==='reader'` appears in the persisted graph AND the re-mounted Reader re-mirrors it after reload (idempotent, not dropped). **★ All four EPIC-6 acceptance metrics moved → EPIC-6 CLOSED.** *Cloud limit:* a fresh-checkout Network canvas is empty, so the book node's live inspector render is not screenshottable headless — the `GRAPH-LEGIBLE` guard carries the roundtrip; on-device is the visual confirm. **▶ NEXT = Strategist promotes the next epic** (node-level lineage OR global cross-app search — both cloud-executable; EPIC-7 Android stays device-gated). | ✅ MET — `PROVENANCE-PERSISTS` 3/3 + `PROVENANCE-ENTITY` 3/3 + Reader graph-legible 1/1 |
| **EPIC-12 target (ACTIVE) — every cross-app creation intent produces a REAL, persistent, reload-durable entity in its home store (no phantom graph nodes): `INTENT-ROUNDTRIP` 0/2 → 2/2** | QA `qa-smoke.mjs` `INTENT-ROUNDTRIP` guard + `sync.test.ts` | **▶ PROMOTED 2026-07-06 — not yet built.** Latent organism-integrity BUG (code-confirmed, `src/lib/core/sync.ts`): `make-note-from` (`:139`) + `add-to-learning` (`:153`) call `g.addNode({type:'note'\|'learning'})` directly, never writing the real store — and `reconcile()` (`:63-65`) prunes any centrally-mirrored node whose `data.sourceId` is absent from its store, so both results vanish (never in Notes/Learning, deleted on the next mutation/reload). `make-task` is graph-only BY DESIGN (Inbox lens; no task store) → unaffected. **Fix:** route the two intents through `useStore.addNote`/`addLearningItem` (the synchronous `subscribe(syncAll)` mirror re-materializes them un-prunably), preserving `data.from` end-to-end for lineage. S1 = note round-trip + guard 0/1 (add `from?` to `Note`; note mirror carries `from`); S2 = learning round-trip 1/1→2/2; S3 = LOCK via a reconcile-survival invariant test. QA confirms `INTENT-ROUNDTRIP 2/2` on green main → retire. **✅ S1 + S2 DONE-CONFIRMED LIVE 2026-07-06 (green main `94ff5f1`) — the acceptance metric MOVED to its target and holds.** First headless drive of both intents (`make-note-from` `92380dc` + `add-to-learning` `94ff5f1` landed since last QA `7e68e1c`): **`INTENT-ROUNDTRIP 2/2 ✅`** — `make-note-from` (`stored=true mirrored=true persisted=true`) + `add-to-learning` (same), each producing a real store entity + un-prunable `sourceId`-keyed mirror surviving a 2nd reload. **The learning axis needed a GUARD fix (my harness, not the product):** it had asserted the item's `from`=the store note id, but the intent honestly writes `from`=the note MIRROR's graph-node id (`NodeActions` resolves by `sourceId` and hands the intent the graph node; reconcile keeps the store id only in `data.sourceId`) — corrected to resolve + match the mirror id. Product verified correct by direct probe. **▶ Only S3 (test-only reconcile-survival LOCK, no runtime change) remains → then EPIC-12 CODE-COMPLETE.** | ✅ target: `INTENT-ROUNDTRIP` 2/2 (note + learning both survive a reload) — **REACHED (S1+S2), S3 locks it** |
| **EPIC-11 target (DONE — retired 2026-07-06) — the whole Empire's NON-colour design tokens re-tune from one place: raw radii/type/easing → `--radius-*`/`--text-*`/`--ease-*` (`offSystemStyle` 56 → 0)** | QA `metrics.mjs` `offSystemStyle` row + `styleAudit.test.mjs` + touched-app render | **✅ EPIC-11 S2 + S3 DONE-CONFIRMED LIVE 2026-07-05 (green main `57262e8`) — the acceptance metric moved twice, both reproduced independently.** First QA since S1 (last QA `ca10d0a` was the S1 confirm; S2 `20bc957` + S3 `4f79ded` + Cakra `57262e8` landed since). `node scripts/metrics.mjs` reproduces **Off-system style `2 (r0/t0/m2)`** exactly on a fresh checkout — down from the S1 baseline of 56: **S2 drove type t42→0** (all raw `font-size`/`fontSize` mapped onto `--text-*`), **S3 drove radii r12→0** (all raw `border-radius`/`borderRadius` mapped onto `--radius-*`). The metric fell **56 → 14 → 2** exactly as ratified (dim-major, heaviest-first); the two survivors are precisely the **S4 motion** targets — `ArtifactGallery.tsx` (`ease-out`) + `Calculator.tsx` (`ease-in-out` pulse). All touched apps (Calculator, MarkdownStudio, Notes, ErrorBoundary, ChatPanel, Toast, ChartBuilder, CommandPalette, Utility) render clean in the smoke (30/30, 0 uncaught). vitest **360/360** (38 files); the two colour audits still 0 and `metrics.mjs --assert-zero` exit 0 (offSystemStyle deliberately NOT locked while non-zero — locks at 0 in S4). **★ S2 + S3 acceptance moved → both done-confirmed, no contradiction. ▶ ONLY S4 remains (motion m2→0 + LOCK) → EPIC-11 CODE-COMPLETE.** | 0 (`offSystemStyle` = 0, then LOCK via `--assert-zero`) |
| **EPIC-10 target (DONE — retired 2026-07-04) — the organism gets a TEMPORAL lens: one newest-first, day-grouped stream merging every entity-birth + every app→app handoff (`TIMELINE`)** | QA `qa-smoke.mjs` guard + `timeline.test.ts` + visual | **✅ EPIC-10 S1 DONE-CONFIRMED LIVE 2026-07-04 (green main `3cfe846`) — the headline `TIMELINE 0/1 → 1/1` reproduced independently.** First QA since S1 landed (last QA `5d45ce8` was EPIC-9 S3; EPIC-9 retired + EPIC-10 promoted + S1 `3cfe846` landed since). The guard seeds two graph-survivable `task` nodes (distinct `meta.created`, two apps) + one `empire-provenance` edge, reloads so both persist stores rehydrate, opens `/app/timeline`, and asserts the two entity rows render newest-`created` first (`ordered`), at least one `[data-timeline-day]` header renders (`grouped`), the seeded edge renders as a `[data-timeline-kind=flow]` row (`flow`), and it all holds after a SECOND reload (`persisted`): **`ordered=true grouped=true flow=true persisted=true` → 1/1 ✅.** Backed by `timeline.test.ts` (15 cases, vitest 292→307). **Visually confirmed** (`app-timeline.png`): the clock-glyph + "The organism's history, one stream · 2 moments" header, a **TODAY** sticky day group, two entity rows (accent dot + title + `dataset` type chip + relative `now`). The pure spine (`buildTimeline`/`groupByDay`/`dayKey`/`relativeDayLabel`) is unit-pinned; this carries the graph+ledger→persist→rehydrate→ordered-render roundtrip jsdom cannot. **★ S1 acceptance moved → S1 done-confirmed, no contradiction.** **✅ S2 DONE-CONFIRMED LIVE 2026-07-04 (green main `a89e87e`) — the acceptance axis GREW independently: `TIMELINE 1/1` now carries a FIFTH axis `filtered=true`** (`ordered=true grouped=true flow=true persisted=true filtered=true`). First QA since S2 landed (last QA `cf62dab` was the S1 confirm; S2 `a89e87e` landed since). S2 added three pure helpers to `timeline.ts` (`filterTimeline`/`timelineFacets`/reused `toggleFacet`) + Kind/App chip rows + a roving ↑/↓/Enter cursor on the flat filtered stream; the guard's new `filtered` axis clicks the `app:goals` facet chip and asserts the stream narrows to ONLY the goals-owned entity — dropping the notes entity + the notes→goals flow (flow app = fromApp = notes) — reproduced without the builder's tree. Backed by `timeline.test.ts` (15→22 cases, vitest 307→314). **Visually confirmed** (`app-timeline.png`): the temporal lens renders the TODAY day-group + two entity rows; *cloud limit:* the Kind/App facet chips sit low-contrast against the dark theme with a 2-moment corpus and don't read prominently in the static shot — the `filtered` axis carries the facet-narrow roundtrip headless + the roving cursor / focus state is unit-pinned. **★ S2 acceptance moved → S2 done-confirmed, no contradiction.** **✅ S3 DONE-CONFIRMED LIVE 2026-07-04 (green main `6a1a0b2`) — the acceptance axis GREW AGAIN independently: `TIMELINE 1/1` now carries a SIXTH axis `descendants=true`** (`ordered=true grouped=true flow=true persisted=true filtered=true descendants=true`). First QA since S3 landed (last QA `b3703ce` was the S2 confirm on `a89e87e`; S3 `6059284` + docs pass `6a1a0b2` landed since). S3 surfaced the long-dormant `childrenOf` walker (built EPIC-9 S1, unused until now) via a new `<NodeDescendants>` mounted on each Timeline entity row beside `<NodeLineage>` — a moment now reads BOTH ways (`↖ ancestry` + `→ spawned`); the guard's new `descendants` axis links the two seeds (`qa-tl-newer.data.from = qa-tl-older`) and asserts the older row surfaces `[data-node-descendants=qa-tl-older]` naming the newer child it spawned — reproduced without the builder's tree. Backed by `NodeDescendants.test.tsx` (+4, vitest 314→318). *Cloud limit:* the expand-and-climb + the `→ spawned` token are on-device interactions — the `descendants` axis carries the render + correct-child-id headless, and the click→open+focus state change is unit-pinned in `NodeDescendants.test.tsx`. **★ S3 acceptance moved → S3 done-confirmed, no contradiction. ★ EPIC-10 CODE-COMPLETE (S1–S3 all shipped + QA-confirmed) → Strategist retires EPIC-10 to DONE + promotes the next epic** (design-system conformance II — cloud-executable; EPIC-7 · Android stays device-gated). | ✅ MET (S1 + S2 + **S3**) — `TIMELINE` 1/1 (`ordered`+`grouped`+`flow`+`persisted`+`filtered`+`descendants`) |
| **EPIC-9 target (CODE-COMPLETE) — per-artifact ancestry is legible + NAVIGABLE: a child node shows its real parent entity, surviving reload, and each hop climbs to its source (`NODE-LINEAGE`)** | QA `qa-smoke.mjs` guard + `nodeLineage.test.ts` + `NodeLineage.test.tsx` + visual | **✅ EPIC-9 S1 DONE-CONFIRMED LIVE 2026-07-03 (green main `fcfa06d`) — the headline `NODE-LINEAGE 0/1 → 1/1` reproduced independently.** First QA since S1 landed (last QA `7ef9a5c` was EPIC-8 S2; EPIC-8 S3 `4e6a78a` + EPIC-9 S1 `fcfa06d` landed since). The guard seeds two graph-survivable `task` nodes (parent + child with `data.from`=parent id), loads `/app/inbox`, asserts a `[data-node-lineage=<parentId>]` element rendering the **parent's real entity title**, then reloads TWICE → still resolves off durable `empire-core-graph`: **`rendered=true title=true persisted=true` → 1/1 ✅.** Backed by `nodeLineage.test.ts` (11 cases, vitest 265→276). **Visually confirmed** (`s1-node-lineage-inbox.png`): the "Draft Q3 roadmap" Inbox row shows the NodeLineage trail `↖ ⌾ Quarterly planning source` (real ancestor entity, not an app name); self-hides with no `data.from`. **★ S1 acceptance moved → S1 done-confirmed, no contradiction.** **✅ S2 DONE-CONFIRMED LIVE 2026-07-03 (green main `f878844`) — the acceptance axis GREW independently: `NODE-LINEAGE 1/1` now carries a FOURTH axis `search=true`** (`rendered=true title=true persisted=true search=true`). S2 dropped `<NodeLineage>` onto the two graph-node-rendering views (Network inspector per-entity list + Search result rows), reusing the S1 walker + component verbatim; the guard's new `search` axis opens `/app/search`, queries "anomaly", and asserts the derived child hit renders `[data-node-lineage=qa-lineage-parent]` — reproduced without the builder's tree. Backed by `nodeLineage.test.ts` (11 cases, vitest 288). *Cloud limit:* the Network-inspector list is a visual/on-device render (canvas node-click headless is fragile) → the Search axis carries the mount roundtrip; the inspector reuses the same unit-pinned component. **★ S2 acceptance moved → S2 done-confirmed, no contradiction.** **✅ S3 DONE-CONFIRMED LIVE 2026-07-03 (green main `0378d8e`) — the acceptance axis GREW AGAIN independently: `NODE-LINEAGE 1/1` now carries a FIFTH axis `clickable=true`** (`rendered=true title=true persisted=true search=true clickable=true`). S3 made each `<NodeLineage>` hop NAVIGABLE — `EntityToken` became a `role="button"` span calling `openEntity(node.meta.app, node.id)` (opens the ancestor's owning app + points `focusedId` at it); the guard's new `clickable` axis asserts the parent hop renders as a live `[role="button"]` targeting the parent entity, then CLICKS it (handler must not throw) — reproduced without the builder's tree. Backed by `NodeLineage.test.tsx` (+4 navigation cases, vitest 288→292). *Cloud limit:* on the `/app/search` route AppShell renders by URL param, not windowStore → the in-app window swap isn't observable headless; the `clickable` axis carries the live click-path + correct parent wiring, and the actual window/focus state change is unit-pinned in `NodeLineage.test.tsx`. **★ S3 acceptance moved → S3 done-confirmed, no contradiction. ★ EPIC-9 CODE-COMPLETE (S1–S3 all shipped + QA-confirmed). ▶ NEXT = Strategist retires EPIC-9 + promotes the next epic** (EPIC-7 · Android stays device-gated). | ✅ MET — `NODE-LINEAGE` 1/1 (S1 + S2 `search` + **S3 `clickable`** axes) |
| **EPIC-8 target (CODE-COMPLETE) — global cross-app search reaches ≥2 apps' entities, grouped by owning app (`GLOBAL-SEARCH`)** | QA `qa-smoke.mjs` guard + `search.test.ts` + visual | **✅ EPIC-8 S1–S2 DONE-CONFIRMED; S3 shipped `4e6a78a` (code-complete).** S2 acceptance MOVED: the guard's `tagOnly` axis passes. First QA after S2 landed (last QA `ce30e4e` was S1 on `ac6af7b`). S2 closed two gaps: **(a)** `nodeBodyText` now flattens the scalar elements of array values → `tags` are searchable; **(b)** `openEntity(appId,nodeId)` deep-links a hit + Notes lands on the focused card (`.focus-land` ring). The `GLOBAL-SEARCH` guard grew a **third seed** — a graph-survivable node carrying `Tessellate` ONLY in `data.tags` — and now reports `book=true task=true twoApps=true **tagOnly=true**`, groups **reader,goals** → **1/1 ✅ reproduced independently**. Backed by `search.test.ts` array-flatten + tag-only cases (**vitest 255→257**). S1's `0/1→1/1` move stands; S2 deepens it (tag/array corpus + land-on-entity). *Cloud limit:* the `openEntity`/focus-land visual is on-device (fresh-checkout corpus is graph-only) — unit-pinned + the guard carries the tag roundtrip. **★ S2 acceptance confirmed moved → S2 done-confirmed, no contradiction.** **▶ NEXT = EPIC-8 S3** (type/app filter chips via a new pure `filterHits` + ↑/↓/Enter keyboard nav + a global summon key — the last EPIC-8 stage; shipping it retires EPIC-8 to DONE). | ✅ MET — `GLOBAL-SEARCH` 1/1 (S1 + S2 axes) |
| Lighthouse — PWA / Perf / A11y | CI (add to a workflow when feasible) | not measured headless | 90 / 90 / 90 |
| **EPIC-4 target — offline-boot guard + Lighthouse PWA ≥ 90 → installability contract** | QA `qa-offline.mjs` + `check-pwa-base.mjs` | **✅ EPIC-4 FULLY DONE (offline ✅ + base ✅ + installable ✅) — confirmed 2026-06-29 on green main `d17f73a`.** `qa-offline.mjs`: warm-load → `setOffline(true)` → **5/5 routes boot cold-offline** (`/`, `/app/clock`, `/app/maps`, `/app/network`, `/app/photos`) from the SW precache; PRECACHE-AUDIT 63 entries (37 JS + 2 CSS), **NO GAP** ✅. Base-path/install-flow (S3) ✅ consistent under `--base=/empire/`. **★ EPIC-4 S4 acceptance CONFIRMED MOVED: `node scripts/check-pwa-base.mjs` → `installable = ✅ (4 icons)`** — manifest passes name+short_name, ≥192 AND ≥512 `any` icon, a maskable icon, standalone display, start_url, background+theme color (the deterministic, offline-checkable realization of *Lighthouse-PWA ≥ 90*). | offline-boots PASS ✅ + installable ✅ — **MET** |
| Open `routine/auto-*` PR age | reviewer log | — | < one review cycle |

## How each routine uses this

- **Builder** — runs `metrics.mjs`, pastes the delta row into the PR body, must not regress any ↓/steady metric.
- **Reviewer** — re-runs it on the merge candidate; a regression is a **hard gate** (block or require justification in the epic).
- **Visual & Smoke QA** — updates the manual rows (routes rendering, organism wiring) every run and confirms the **active epic's target metric actually moved** before marking a stage done.
- **Strategist** — picks the active epic to maximize capability gradient = (metric gain ÷ stages of effort).
- **Daily Digest** — reports today's deltas as the headline (is the fleet leaping or stalling?).

> Keep this table's "Baseline/Target" columns honest: when a target is hit, raise
> it or retire the row. A flat metric for 3+ runs is a signal for the Routine
> Optimizer that the active epic is in a local minimum.
